# tools/ 目录树

> user-neo4j MCP server 的工具定义目录，每个 JSON 描述一个可被调用的 Neo4j Cypher 工具的入参 schema。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `read_neo4j_cypher.json` | 只读 Cypher 查询工具的定义与参数 schema |
| `write_neo4j_cypher.json` | 写入 Cypher 语句工具的定义与参数 schema |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
