# Agent Trace Visualization

MemoChat AI Agent traces expose a debuggable timeline with event durations, masked prompt/request previews, model output previews, and collapsible tool request/response details. The feature keeps sensitive keys such as api_key, token, password, secret, and authorization masked by default while preserving enough context for debugging and audit.
