# 上下文

## 任务

用户要求继续加入其他规则，例如“如果改动了项目某个地方，就要协同更改涉及到的文档，做到实时更新”。

## 相关文件

- `AGENTS.md`：仓库级规则应包含文档同步约束。
- `skills/SKILL.md`：主任务流水线应在计划、评估、实现、复查阶段体现文档同步。
- `skills/task.md`：常规实现 workflow 应要求报告文档同步情况。
- `skills/PROMPTS.md`：委托 agent 共享规则和计划模板应包含文档同步项。

## 数据和服务依赖

无。此次只补充文档规则，不触碰 Docker、数据库、运行时配置或业务代码。

## 验证建议

- 搜索“文档同步”确认规则落点。
- 运行 `git diff --check` 检查 Markdown 空白。
