# AI Agent UI polish

MemoChat AI Agent screens use the same glass and chat-bubble language as private chat. The AI Agent model list loads through the Gate GET route, shows the locally configured Ollama `qwen3:4b` model, and the knowledge-base panel stays inside its parent at narrow sizes. The R18 shell uses transparent pink glass surfaces and rounded controls instead of default white controls.
