# DeepSeek Model Selection

MemoChat keeps a user's API model selection stable across model refreshes and automatically switches to a newly registered external API model when the current selection is still the built-in Ollama default.
