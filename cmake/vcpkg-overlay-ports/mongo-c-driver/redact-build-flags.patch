diff --git a/src/libmongoc/CMakeLists.txt b/src/libmongoc/CMakeLists.txt
index 724921c..1075f64 100644
--- a/src/libmongoc/CMakeLists.txt
+++ b/src/libmongoc/CMakeLists.txt
@@ -479,7 +479,9 @@ endif ()
 
 cmake_path(GET CMAKE_C_COMPILER FILENAME MONGOC_CC)
-set (MONGOC_USER_SET_CFLAGS ${CMAKE_C_FLAGS})
-set (MONGOC_USER_SET_LDFLAGS ${CMAKE_EXE_LINKER_FLAGS})
+# Compiler/linker flags can contain absolute build roots. They are not needed
+# for protocol behavior and must not be published in handshake metadata.
+set (MONGOC_USER_SET_CFLAGS "")
+set (MONGOC_USER_SET_LDFLAGS "")
 
 if (ENABLE_TRACING)
    set (MONGOC_TRACE 1)
