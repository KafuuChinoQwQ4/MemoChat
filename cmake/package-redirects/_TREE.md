# package-redirects/ 目录树

> CMake 包查找重定向配置，覆盖/桥接某些第三方包的 config 以适配本项目的依赖解析。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `unofficial-utf8proc-config.cmake` | 为 utf8proc 提供非官方包名的 config 重定向 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
