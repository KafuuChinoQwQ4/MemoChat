# ai/ 目录树

> AI 路由模块的具体实现，把 AI 相关请求挂载到网关路由注册表。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `AIRouteModule.cpp` | AI 路由模块实现：注册 AI 请求处理端点。 |
| `AIRouteModule.h` | AI 路由模块接口声明。 |
| `AIRouteSchemas.cpp` | AI 稳定 mapper 路由只读 schema descriptor 列表。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
