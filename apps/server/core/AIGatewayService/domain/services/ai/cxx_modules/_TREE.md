# cxx_modules/ 目录树

> AI 网关服务层和公共 DTO 层的 GNU C++ module interface 文件。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `AIPublicDto.cppm` | AI 公共 DTO 动态 JSON fallback 与可选字段判断算法 module。 |
| `AIService.cppm` | AI 网关服务响应常量、默认查询值和空内容判定算法 module。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
