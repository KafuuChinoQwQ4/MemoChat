# app/ 目录树

> CallGatewayServer 进程入口，注册 Call 路由 Profile 并以 GateDomainServer 骨架启动（默认端口 8097）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `CallGatewayServer.cpp` | CallGatewayServer main 入口，通话信令网关 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
