# cxx_modules/ 目录树

> Moments 路由模块 C++ modules 接口单元，承载路由注册与 schema 字面量算法。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `MomentsRouteRegistration.cppm` | 导出 Moments 路由注册使用的 HTTP method 与 path 字面量算法。 |
| `MomentsRouteSchema.cppm` | 导出 Moments 路由 schema 使用的 method、path、route name 与 DTO type-name 字面量算法。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
