# cxx_modules/ 目录树

> 媒体路由模块的 C++ modules 接口单元，用于逐步迁移路由注册与 schema 字面量决策。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `MediaRouteRegistration.cppm` | 导出媒体路由注册使用的 HTTP method 与 path 字面量算法模块。 |
| `MediaRouteSchema.cppm` | 导出媒体路由 schema 使用的 method、path、route name 与 DTO type-name 字面量算法模块。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
