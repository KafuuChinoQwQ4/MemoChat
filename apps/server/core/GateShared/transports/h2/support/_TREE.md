# support/ 目录树

> HTTP/2 各业务处理器的支撑工具，封装鉴权/通话/媒体/朋友圈/资料的辅助逻辑。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `Http2AuthSupport.cpp` | H2 鉴权支撑逻辑实现。 |
| `Http2AuthSupport.h` | H2 鉴权支撑声明。 |
| `Http2CallSupport.cpp` | H2 通话支撑逻辑实现。 |
| `Http2CallSupport.h` | H2 通话支撑声明。 |
| `Http2MediaSupport.h` | H2 媒体支撑声明（头文件实现）。 |
| `Http2MomentsSupport.cpp` | H2 朋友圈支撑逻辑实现。 |
| `Http2MomentsSupport.h` | H2 朋友圈支撑声明。 |
| `Http2ProfileSupport.h` | H2 资料支撑声明（头文件实现）。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
