# routes/ 目录树

> HTTP/2 路由表装配，将路径挂载到本地处理器或统一 RouteRegistry。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `Http2Routes.cpp` | H2 路由注册与统一路由转发实现。 |
| `Http2Routes.h` | H2 路由注册声明。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
