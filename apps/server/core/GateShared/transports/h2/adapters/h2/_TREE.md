# h2/ 目录树

> 将 HTTP/2 请求与响应适配到 GateShared 统一路由注册表的适配器。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `H2RouteAdapter.cpp` | HTTP/2 路由适配器实现，包含统一响应和文件体转换。 |
| `H2RouteAdapter.h` | HTTP/2 路由适配器接口声明。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
