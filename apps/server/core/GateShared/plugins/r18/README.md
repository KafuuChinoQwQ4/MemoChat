# GateServer R18 Plugins

This module owns helper targets used by the GateServer R18 source pipeline:

- `R18PluginHost`
- `R18MockSourcePlugin`

These helper targets are buildable by default and are independent from the legacy `GateServerHttp1.1` standalone option.
