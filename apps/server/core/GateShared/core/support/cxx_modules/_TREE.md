# cxx_modules/ 目录树

> GateShared support 层的 C++ modules 接口单元，用于逐步迁移共享支撑算法。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `BearerAccessAuth.cppm` | 导出 Authorization Bearer 头解析与 header 名大小写匹配算法模块。 |
| `UserTokenValidator.cppm` | 导出用户 token 校验前置 guard 与 Redis value 接受判断算法模块。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
