# cxx_modules/ 目录树

> GateShared Redis 缓存层的 GNU C++ module interface 文件。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `RedisMgr.cppm` | Redis 管理器连接池默认值、reply 判定与批量 guard 算法 module。 |
| `RedisPipeline.cppm` | Redis pipeline 登录缓存 key 前缀字面量 module。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
