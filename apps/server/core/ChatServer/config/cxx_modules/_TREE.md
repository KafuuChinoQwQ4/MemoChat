# cxx_modules/ 目录树

> ChatServer 配置层的 C++ module interface units，用于导出不依赖标准库实体的纯算法。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `ChatConfig.cppm` | 导出 ChatServer 配置的 ASCII 大小写归一化、布尔 flag 解析和整数钳制算法 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
