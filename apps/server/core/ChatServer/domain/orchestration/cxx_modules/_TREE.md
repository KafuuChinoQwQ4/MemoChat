# cxx_modules/ 目录树

> 编排层 C++ module interface unit，导出 primitive-only 的运行时装配决策算法。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `ChatHandlerRegistrar.cppm` | 导出 ChatHandlerRegistrars 各类处理器注册数量和空 dispatcher 判定算法 |
| `ChatRuntimeComposition.cppm` | 导出 ChatRuntimeComposition 后端选择与 fallback warning 判定算法 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
