# random/ 目录树

> 以操作系统返回码提供安全随机 UUID，避免依赖抛异常的标准熵源。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `Uuid.hpp` | 通过 BCrypt 或 `/dev/urandom` 显式生成 RFC 4122 v4 UUID |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
