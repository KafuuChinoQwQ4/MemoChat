# json/ 目录树

> Glaze JSON 库的兼容封装、typed DTO 编解码与对应测试辅助头，统一各服务的 JSON 序列化方式。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `GlazeCompat.h` | Glaze JSON 序列化兼容层 |
| `GlazeCompat_test.h` | Glaze 兼容层的测试辅助定义 |
| `TypedJsonCodec.h` | typed DTO 的 Glaze JSON 编解码封装 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
