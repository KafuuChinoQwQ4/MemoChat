# app/ 目录树

> LoginServer 进程入口，注册 Login 路由 Profile 并以 GateDomainServer 骨架启动（默认端口 8102），签发会话 token 与登录票据。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `LoginServer.cpp` | LoginServer main 入口，鉴权与 token 签发 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
