# app/ 目录树

> AccountServer 进程入口，注册 Account 路由 Profile 并以 GateDomainServer 骨架启动（默认端口 8103）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `AccountServer.cpp` | AccountServer main 入口，承载资料写入与身份缓存 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
