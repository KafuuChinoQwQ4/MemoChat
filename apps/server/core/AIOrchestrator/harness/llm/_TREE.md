# llm/ 目录树

> harness 内的 LLM 服务封装层，向上层提供统一的模型调用入口。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `README.md` | LLM 层说明文档。 |
| `__init__.py` | 包初始化。 |
| `service.py` | LLM 服务实现，负责 provider 选择、动态 API 接入和上下文窗口 token 预算保护。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
