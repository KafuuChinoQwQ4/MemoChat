# runtime/ 目录树

> R18 外壳的 QML 运行时 JS 辅助库，提供 URL 归一化、标签处理等纯函数逻辑。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `R18ShellRuntime.js` | 库式 JS：绝对 URL 拼接、标签数组归一化等外壳辅助函数 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
