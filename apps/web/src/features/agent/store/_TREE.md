# store/ 目录树

> Web AI 对话的流式状态 store。

## 文件

| 文件 | 作用 |
| --- | --- |
| `agentStore.js` | `agentStore.ts` 的生成 JS companion。 |
| `agentStore.ts` | Zustand store，保存 AI 流式请求的 streaming、error 和增量文本状态。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
