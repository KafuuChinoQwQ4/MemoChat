# random/ 目录树

> 测试返回码式安全随机 UUID 生成器。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `CMakeLists.txt` | 注册 UUID GTest 目标 |
| `main.cpp` | GTest 入口 |
| `UuidTest.cpp` | 验证 UUID v4 格式、variant 与随机唯一性 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
