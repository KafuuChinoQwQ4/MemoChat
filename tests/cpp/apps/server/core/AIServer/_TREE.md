# AIServer/ 目录树

> 测试 AIServer 服务，当前覆盖 AI 服务 JSON 映射逻辑。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `AIServiceJsonMapperTest.cpp` | 验证 AI 服务请求/响应的 JSON 映射 |
| `CMakeLists.txt` | 注册该测试目标的 CMake 配置 |
| `main.cpp` | GTest 测试入口（main 函数） |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
