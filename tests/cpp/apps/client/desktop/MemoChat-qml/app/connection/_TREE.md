# connection/ 目录树

> 测试客户端 app/connection 的聊天连接策略（AppChatConnectionPolicy 端点选择/回退逻辑）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `AppChatConnectionPolicyTest.cpp` | 验证聊天连接策略的端点解析与回退判定 |
| `CMakeLists.txt` | 注册该测试目标的 CMake 配置 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
