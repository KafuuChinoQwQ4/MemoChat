# VarifyServer/ 目录树

> 测试 VarifyServer（验证码）服务的契约（邮件投递失败处理、C++-only 实现约束）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `test_email_delivery_failure_contract.py` | 校验邮件投递失败处理契约 |
| `test_varifyserver_cpp_only_contract.py` | 校验 VarifyServer 仅 C++ 实现的约束契约 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
