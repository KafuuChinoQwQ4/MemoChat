# GateServer/ 目录树

> 测试 GateServer 网关服务的结构与微服务拆分契约（路由清单、DB/服务拆分、媒体上传、桌宠边界等）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `test_gate_compatibility_inventory.py` | 校验网关兼容性接口清单 |
| `test_gateserver_db_split_contract.py` | 校验 GateServer 数据库拆分契约 |
| `test_gateserver_route_inventory.py` | 校验 GateServer 路由清单 |
| `test_gateserver_service_split_contract.py` | 校验 GateServer 服务拆分契约 |
| `test_gateserver_structure.py` | 校验 GateServer 源码物理布局 |
| `test_media_upload_contract.py` | 校验网关媒体上传契约 |
| `test_pet_gate_boundary_contract.py` | 校验桌宠在网关侧的职责边界契约 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
