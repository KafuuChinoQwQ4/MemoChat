# MemoChat 数据库架构文档

> 文档版本：2026-03-23

---

## 一、数据库概览

MemoChat 采用多数据库架构，针对不同数据特性选择最适合的存储引擎：

| 数据库 | 引擎 | 用途 | 数据特点 |
|--------|------|------|----------|
| PostgreSQL | PostgreSQL 16 | 用户、关系、群组、会话 | 结构化、强一致性 |
| MongoDB | MongoDB 7 | 聊天消息历史 | 半结构化、高写入 |
| Redis | Redis 7 | 缓存、会话、实时状态 | 内存型、低延迟 |

### 1.1 PostgreSQL Schema

- **Schema 名**: `memo` (可通过配置修改)
- **用途**: 业务主数据存储

### 1.2 MongoDB Collections

- **用途**: 聊天消息历史存储，支持海量消息查询

### 1.3 Redis Keys

- **用途**: 缓存、Session、实时状态、发布订阅

---

## 二、PostgreSQL 表结构

### 2.1 用户表 (user)

存储用户基本信息。

```sql
CREATE TABLE IF NOT EXISTS "user" (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    uid integer NOT NULL DEFAULT 0,           -- 用户唯一ID
    name varchar(255) NOT NULL DEFAULT '',    -- 用户名
    email varchar(255) NOT NULL DEFAULT '',    -- 邮箱
    pwd varchar(255) NOT NULL DEFAULT '',     -- 密码哈希
    nick varchar(255) NOT NULL DEFAULT '',    -- 昵称
    "desc" varchar(255) NOT NULL DEFAULT '',  -- 个人描述
    sex integer NOT NULL DEFAULT 0,            -- 性别 0=未知 1=男 2=女
    icon varchar(255) NOT NULL DEFAULT '',     -- 头像URL
    user_id varchar(10),                       -- 用户名ID (唯一)
    CONSTRAINT uq_user_uid UNIQUE (uid),
    CONSTRAINT uq_user_email UNIQUE (email),
    CONSTRAINT uq_user_user_id UNIQUE (user_id)
);
```

**索引**:
- `idx_user_name` - 按用户名搜索

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| uid | integer | 全局唯一用户ID |
| name | varchar | 用户名（登录名） |
| email | varchar | 邮箱地址 |
| pwd | varchar | BCrypt 哈希密码 |
| nick | varchar | 显示昵称 |
| desc | varchar | 个人签名 |
| sex | integer | 性别 0=未知 1=男 2=女 |
| icon | varchar | 头像URL |
| user_id | varchar | 短用户名ID |

### 2.2 UID 生成器 (user_id)

用于生成全局唯一的用户ID。

```sql
CREATE TABLE IF NOT EXISTS "user_id" (
    id integer PRIMARY KEY
);
```

### 2.3 好友关系表 (friend)

存储双向好友关系。

```sql
CREATE TABLE IF NOT EXISTS friend (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    self_id integer NOT NULL,      -- 自己的uid
    friend_id integer NOT NULL,    -- 好友的uid
    back varchar(255) NOT NULL DEFAULT '',  -- 备注名
    CONSTRAINT uq_friend_self_friend UNIQUE (self_id, friend_id)
);
```

**索引**:
- `idx_friend_self_id_friend_id` - 复合索引加速查询

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| self_id | integer | 用户自己的uid |
| friend_id | integer | 好友的uid |
| back | varchar | 备注名 |

### 2.4 好友申请表 (friend_apply)

存储好友申请记录。

```sql
CREATE TABLE IF NOT EXISTS friend_apply (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    from_uid integer NOT NULL,      -- 申请方uid
    to_uid integer NOT NULL,        -- 被申请方uid
    status smallint NOT NULL DEFAULT 0,  -- 状态 0=待处理 1=已同意 2=已拒绝
    CONSTRAINT uq_friend_apply_from_to UNIQUE (from_uid, to_uid)
);
```

**索引**:
- `idx_friend_apply_to_uid_id_from_uid` - 加速查询待处理申请

**状态说明**:
| 状态值 | 说明 |
|--------|------|
| 0 | 待处理 |
| 1 | 已同意 |
| 2 | 已拒绝 |

### 2.5 好友申请标签表 (friend_apply_tag)

存储好友申请的附加标签。

```sql
CREATE TABLE IF NOT EXISTS friend_apply_tag (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    from_uid integer NOT NULL,
    to_uid integer NOT NULL,
    tag varchar(64) NOT NULL,
    CONSTRAINT uq_friend_apply_tag UNIQUE (from_uid, to_uid, tag)
);
```

### 2.6 好友标签表 (friend_tag)

存储用户对好友的分类标签。

```sql
CREATE TABLE IF NOT EXISTS friend_tag (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    self_id integer NOT NULL,
    friend_id integer NOT NULL,
    tag varchar(64) NOT NULL,
    CONSTRAINT uq_friend_tag UNIQUE (self_id, friend_id, tag)
);
```

### 2.7 群组表 (chat_group)

存储群组基本信息。

```sql
CREATE TABLE IF NOT EXISTS chat_group (
    group_id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    group_code varchar(10),                     -- 群号
    name varchar(64) NOT NULL,                  -- 群名称
    owner_uid integer NOT NULL,                  -- 群主uid
    announcement text,                           -- 群公告
    member_limit integer NOT NULL DEFAULT 200,  -- 成员上限
    is_all_muted boolean NOT NULL DEFAULT false, -- 全员禁言
    status smallint NOT NULL DEFAULT 1,         -- 状态 1=正常 0=解散
    created_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    icon varchar(512),                          -- 群头像
    CONSTRAINT uq_chat_group_code UNIQUE (group_code)
);
```

**索引**:
- `idx_chat_group_owner_uid` - 按群主查询
- `idx_chat_group_status` - 按状态查询

### 2.8 群组成员表 (chat_group_member)

存储群组成员信息。

```sql
CREATE TABLE IF NOT EXISTS chat_group_member (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    group_id bigint NOT NULL,
    uid integer NOT NULL,
    role smallint NOT NULL DEFAULT 1,       -- 角色 1=普通成员 2=管理员 3=群主
    mute_until bigint NOT NULL DEFAULT 0,    -- 禁言截止时间戳
    join_source smallint NOT NULL DEFAULT 0, -- 入群方式 0=直接加入 1=邀请
    status smallint NOT NULL DEFAULT 1,      -- 状态 1=正常 0=已退出
    joined_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_chat_group_member UNIQUE (group_id, uid)
);
```

**索引**:
- `idx_chat_group_member_uid_status` - 按用户查询其群组
- `idx_chat_group_member_group_status` - 按群组查询成员

**角色说明**:
| 角色值 | 说明 |
|--------|------|
| 1 | 普通成员 |
| 2 | 管理员 |
| 3 | 群主 |

### 2.9 入群申请表 (chat_group_apply)

存储入群申请记录。

```sql
CREATE TABLE IF NOT EXISTS chat_group_apply (
    apply_id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    group_id bigint NOT NULL,
    applicant_uid integer NOT NULL,      -- 申请人uid
    inviter_uid integer NOT NULL DEFAULT 0, -- 邀请人uid
    type smallint NOT NULL DEFAULT 1,   -- 类型 1=申请入群 2=邀请入群
    status smallint NOT NULL DEFAULT 0, -- 状态 0=待处理 1=已同意 2=已拒绝
    reason varchar(128) NOT NULL DEFAULT '', -- 申请理由
    reviewer_uid integer NOT NULL DEFAULT 0, -- 审核人uid
    created_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**索引**:
- `idx_chat_group_apply_group_status` - 按群组和状态查询申请
- `idx_chat_group_apply_applicant` - 按申请人查询
- `idx_chat_group_apply_inviter` - 按邀请人查询

### 2.10 群消息表 (chat_group_msg)

存储群聊消息（仅索引数据，详细内容在 MongoDB）。

```sql
CREATE TABLE IF NOT EXISTS chat_group_msg (
    server_msg_id bigint GENERATED BY DEFAULT AS IDENTITY,
    msg_id varchar(64) PRIMARY KEY,         -- 消息ID
    group_id bigint NOT NULL,                -- 群ID
    from_uid integer NOT NULL,               -- 发送者uid
    msg_type varchar(16) NOT NULL,           -- 消息类型
    content text NOT NULL,                   -- 消息内容
    mentions_json jsonb,                      -- @提及用户列表
    created_at bigint NOT NULL,              -- 创建时间戳
    group_seq bigint NOT NULL DEFAULT 0,     -- 群内序号
    reply_to_server_msg_id bigint NOT NULL DEFAULT 0, -- 回复的消息ID
    forward_meta_json jsonb,                 -- 转发元数据
    edited_at_ms bigint NOT NULL DEFAULT 0,  -- 编辑时间
    deleted_at_ms bigint NOT NULL DEFAULT 0, -- 删除时间
    CONSTRAINT uq_chat_group_msg_server_msg_id UNIQUE (server_msg_id),
    CONSTRAINT uq_chat_group_msg_group_seq UNIQUE (group_id, group_seq)
);
```

**索引**:
- `idx_chat_group_msg_group_created` - 按群组和时间查询
- `idx_chat_group_msg_group_seq` - 按群组序号查询

### 2.11 群消息扩展表 (chat_group_msg_ext)

存储群消息的扩展信息（如文件名、大小等）。

```sql
CREATE TABLE IF NOT EXISTS chat_group_msg_ext (
    msg_id varchar(64) PRIMARY KEY,
    file_name varchar(255) NOT NULL DEFAULT '',
    mime varchar(127) NOT NULL DEFAULT '',
    size integer NOT NULL DEFAULT 0
);
```

### 2.12 私聊消息表 (chat_private_msg)

存储私聊消息索引（详细内容在 MongoDB）。

```sql
CREATE TABLE IF NOT EXISTS chat_private_msg (
    msg_id varchar(64) PRIMARY KEY,
    conv_uid_min integer NOT NULL,           -- 对话用户ID小者
    conv_uid_max integer NOT NULL,           -- 对话用户ID大者
    from_uid integer NOT NULL,               -- 发送者uid
    to_uid integer NOT NULL,                 -- 接收者uid
    content text NOT NULL,                   -- 消息内容
    created_at bigint NOT NULL,              -- 创建时间戳
    reply_to_server_msg_id bigint NOT NULL DEFAULT 0,
    forward_meta_json jsonb,                  -- 转发元数据
    edited_at_ms bigint NOT NULL DEFAULT 0,
    deleted_at_ms bigint NOT NULL DEFAULT 0
);
```

**索引**:
- `idx_chat_private_msg_conv_created` - 按对话和时间查询
- `idx_chat_private_msg_to_created` - 按接收者和时间查询

### 2.13 对话列表表 (chat_dialog)

存储用户的对话列表。

```sql
CREATE TABLE IF NOT EXISTS chat_dialog (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    owner_uid integer NOT NULL,              -- 所有者uid
    dialog_type varchar(16) NOT NULL,        -- 对话类型 private/group
    peer_uid integer NOT NULL DEFAULT 0,      -- 私聊对方uid
    group_id bigint NOT NULL DEFAULT 0,       -- 群ID
    pinned_rank integer NOT NULL DEFAULT 0,   -- 置顶排序
    draft_text varchar(2000) NOT NULL DEFAULT '', -- 草稿
    mute_state smallint NOT NULL DEFAULT 0,  -- 静音状态
    last_msg_preview varchar(255) NOT NULL DEFAULT '', -- 最后消息预览
    last_msg_ts bigint NOT NULL DEFAULT 0,   -- 最后消息时间
    unread_count integer NOT NULL DEFAULT 0, -- 未读数
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_chat_dialog_owner UNIQUE (owner_uid, dialog_type, peer_uid, group_id)
);
```

**索引**:
- `idx_chat_dialog_owner` - 按用户查询
- `idx_chat_dialog_owner_type` - 按用户和类型查询

### 2.14 对话元数据表 (chat_dialog_meta)

存储对话的元数据。

```sql
CREATE TABLE IF NOT EXISTS chat_dialog_meta (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    owner_uid integer NOT NULL,
    dialog_type varchar(16) NOT NULL,
    peer_uid integer NOT NULL DEFAULT 0,
    group_id bigint NOT NULL DEFAULT 0,
    pinned_rank integer NOT NULL DEFAULT 0,
    draft_text varchar(2000) NOT NULL DEFAULT '',
    mute_state smallint NOT NULL DEFAULT 0,
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_chat_dialog_meta_owner UNIQUE (owner_uid, dialog_type, peer_uid, group_id)
);
```

### 2.15 私聊已读状态表 (chat_private_read_state)

存储私聊已读状态。

```sql
CREATE TABLE IF NOT EXISTS chat_private_read_state (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    uid integer NOT NULL,                    -- 用户uid
    peer_uid integer NOT NULL,               -- 对方uid
    read_ts bigint NOT NULL DEFAULT 0,       -- 已读时间戳
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_chat_private_read_state UNIQUE (uid, peer_uid)
);
```

### 2.16 群聊已读状态表 (chat_group_read_state)

存储群聊已读状态。

```sql
CREATE TABLE IF NOT EXISTS chat_group_read_state (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    uid integer NOT NULL,
    group_id bigint NOT NULL,
    read_ts bigint NOT NULL DEFAULT 0,
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_chat_group_read_state UNIQUE (uid, group_id)
);
```

### 2.17 群管理权限表 (chat_group_admin_permission)

存储群管理员权限。

```sql
CREATE TABLE IF NOT EXISTS chat_group_admin_permission (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    group_id bigint NOT NULL,
    uid integer NOT NULL,
    permission_bits bigint NOT NULL DEFAULT 0, -- 权限位图
    updated_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_chat_group_admin_permission UNIQUE (group_id, uid)
);
```

### 2.18 媒体资产表 (chat_media_asset)

存储媒体文件元数据。

```sql
CREATE TABLE IF NOT EXISTS chat_media_asset (
    media_id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    media_key varchar(64) NOT NULL,          -- 媒体唯一键
    owner_uid integer NOT NULL,               -- 所有者uid
    media_type varchar(16) NOT NULL,          -- 媒体类型 image/video/file
    origin_file_name varchar(255) NOT NULL,   -- 原始文件名
    mime varchar(127) NOT NULL DEFAULT '',
    size_bytes bigint NOT NULL DEFAULT 0,
    storage_provider varchar(32) NOT NULL DEFAULT 'local', -- 存储提供者
    storage_path varchar(1024) NOT NULL,      -- 存储路径
    created_at_ms bigint NOT NULL,
    deleted_at_ms bigint NOT NULL DEFAULT 0,
    status smallint NOT NULL DEFAULT 1,       -- 状态 1=正常 0=已删除
    CONSTRAINT uq_chat_media_asset_key UNIQUE (media_key)
);
```

**索引**:
- `idx_chat_media_asset_owner_created` - 按所有者和时间查询

### 2.19 通话会话表 (chat_call_session)

存储音视频通话记录。

```sql
CREATE TABLE IF NOT EXISTS chat_call_session (
    call_id varchar(64) PRIMARY KEY,
    room_name varchar(128) NOT NULL,         -- LiveKit 房间名
    call_type varchar(16) NOT NULL,          -- 通话类型 audio/video
    caller_uid integer NOT NULL,              -- 主叫uid
    callee_uid integer NOT NULL,              -- 被叫uid
    state varchar(32) NOT NULL,               -- 状态 pending/accepted/ended
    started_at_ms bigint NOT NULL,            -- 开始时间
    accepted_at_ms bigint NOT NULL DEFAULT 0, -- 接听时间
    ended_at_ms bigint NOT NULL DEFAULT 0,   -- 结束时间
    expires_at_ms bigint NOT NULL DEFAULT 0, -- 过期时间
    duration_sec integer NOT NULL DEFAULT 0,  -- 通话时长
    reason varchar(64) NOT NULL DEFAULT '',  -- 结束原因
    trace_id varchar(64) NOT NULL DEFAULT '', -- 链路追踪ID
    updated_at_ms bigint NOT NULL DEFAULT 0
);
```

**索引**:
- `idx_chat_call_session_caller` - 按主叫查询
- `idx_chat_call_session_callee` - 按被叫查询
- `idx_chat_call_session_state` - 按状态查询

**状态说明**:
| 状态值 | 说明 |
|--------|------|
| pending | 等待接听 |
| accepted | 已接听 |
| ended | 已结束 |
| cancelled | 已取消 |
| rejected | 已拒绝 |

### 2.20 聊天事件发件箱表 (chat_event_outbox)

用于 Kafka 事件可靠投递的发件箱模式。

```sql
CREATE TABLE IF NOT EXISTS chat_event_outbox (
    event_id varchar(64) PRIMARY KEY,
    event_type varchar(32) NOT NULL,          -- 事件类型
    topic varchar(64) NOT NULL,               -- Kafka Topic
    payload jsonb NOT NULL,                   -- 事件载荷
    status smallint NOT NULL DEFAULT 0,        -- 状态 0=待发送 1=已发送 2=失败
    retry_count integer NOT NULL DEFAULT 0,    -- 重试次数
    created_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sent_at timestamptz
);
```

---

## 三、MongoDB Collections

### 3.1 私聊消息集合 (private_messages)

存储私聊消息完整内容。

```javascript
{
  "_id": ObjectId,
  "msg_id": "msg-uuid-123",       // 消息ID
  "from_uid": 12345,              // 发送者uid
  "to_uid": 67890,                // 接收者uid
  "content": "Hello!",            // 消息内容
  "msg_type": "text",             // 消息类型 text/image/file/audio/video
  "extra": {                      // 扩展信息
    "file_name": "",
    "file_size": 0,
    "mime": ""
  },
  "client_time": 1711180800000,   // 客户端时间
  "server_time": 1711180800001,   // 服务端时间
  "edited_at": null,              // 编辑时间
  "deleted_at": null,              // 删除时间
  "read_at": null                 // 已读时间
}
```

### 3.2 群聊消息集合 (group_messages)

存储群聊消息完整内容。

```javascript
{
  "_id": ObjectId,
  "msg_id": "msg-uuid-456",
  "group_id": 10001,              // 群ID
  "from_uid": 12345,              // 发送者uid
  "content": "Hello everyone!",
  "msg_type": "text",
  "mentions": [67890, 11111],     // @提及的用户
  "reply_to": null,               // 回复的消息ID
  "forward_from": null,            // 转发来源
  "client_time": 1711180800000,
  "server_time": 1711180800001,
  "edited_at": null,
  "deleted_at": null
}
```

---

## 四、Redis Keys

### 4.1 会话缓存

| Key Pattern | 类型 | 说明 | TTL |
|-------------|------|------|-----|
| `session:{uid}:{device_id}` | Hash | 用户会话信息 | 24h |
| `login_ticket:{ticket}` | String | 登录票据验证 | 20s |
| `verify_code:{email}` | String | 验证码 | 300s |

### 4.2 在线状态

| Key Pattern | 类型 | 说明 | TTL |
|-------------|------|------|-----|
| `online:{uid}` | String | 用户在线状态 | 60s |
| `chatserver:{uid}` | String | 用户所在 ChatServer | 60s |

### 4.3 关系缓存

| Key Pattern | 类型 | 说明 | TTL |
|-------------|------|------|-----|
| `friend:{uid}` | Set | 用户好友列表 | 1h |
| `group:{group_id}` | Set | 群组成员列表 | 1h |

### 4.4 消息缓存

| Key Pattern | 类型 | 说明 | TTL |
|-------------|------|------|-----|
| `dialog:{uid}` | Sorted Set | 用户对话列表 | 1h |
| `msg:{msg_id}` | Hash | 消息详情缓存 | 5min |

### 4.5 发布订阅通道

| Channel | 说明 |
|---------|------|
| `chat:notify:{uid}` | 用户消息通知 |
| `group:notify:{group_id}` | 群组消息通知 |
| `presence:{uid}` | 用户在线状态变更 |

---

## 五、索引优化

### 5.1 PostgreSQL 索引策略

| 表名 | 索引类型 | 索引列 | 用途 |
|------|---------|--------|------|
| user | UNIQUE | email | 邮箱唯一约束 |
| user | UNIQUE | uid | UID 唯一约束 |
| user | UNIQUE | user_id | 用户名唯一约束 |
| friend | UNIQUE | (self_id, friend_id) | 好友关系唯一约束 |
| friend | INDEX | (self_id, friend_id) | 好友查询 |
| chat_group_member | UNIQUE | (group_id, uid) | 群成员唯一约束 |
| chat_group_member | INDEX | (uid, status) | 用户所在群查询 |
| chat_private_msg | INDEX | (conv_uid_min, conv_uid_max, created_at) | 私聊历史查询 |
| chat_group_msg | INDEX | (group_id, group_seq) | 群消息序号查询 |

### 5.2 MongoDB 索引策略

| Collection | 索引类型 | 索引列 | 用途 |
|------------|---------|--------|------|
| private_messages | INDEX | (from_uid, to_uid, server_time) | 私聊历史 |
| private_messages | INDEX | (msg_id) | 消息详情查询 |
| group_messages | INDEX | (group_id, server_time) | 群聊历史 |
| group_messages | INDEX | (msg_id) | 消息详情查询 |

---

## 六、数据迁移

### 6.1 迁移脚本位置

```
migrations/
├── postgresql/
│   ├── business/
│   │   ├── 001_baseline.sql
│   │   └── 002_chat_event_outbox.sql
│   └── memo_ops/
│       └── 001_baseline.sql
└── mongodb/
    └── (待扩展)
```

### 6.2 迁移命令

```powershell
# PostgreSQL 迁移
powershell -ExecutionPolicy Bypass -File scripts\init_postgresql_schema.ps1

# MongoDB 迁移（待实现）
mongosh < migrations/mongodb/001_baseline.js
```

---

## 七、数据生命周期管理

### 7.1 消息数据生命周期

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         消息数据生命周期                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  实时消息 (0-7天)                                                          │
│  ────────────────────────────────────────────────────────────────────────    │
│  │ 最新聊天消息存在 MongoDB 中，支持实时查询和拉取                         │
│  │ PostgreSQL 中只存索引（msg_id, sender, receiver, time）                │
│                                                                              │
│  历史消息 (7-365天)                                                        │
│  ────────────────────────────────────────────────────────────────────────    │
│  │ 超过 7 天的消息仍然保存在 MongoDB 中                                   │
│  │ 但可以通过历史记录接口访问                                              │
│  │ PostgreSQL 索引保留完整用于翻页和跳转查询                               │
│                                                                              │
│  归档清理 (>365天)                                                          │
│  ────────────────────────────────────────────────────────────────────────    │
│  │ 超过 365 天的消息标记为 deleted_at                                     │
│  │ 物理删除使用 TTL Index 或定期批任务                                    │
│  │ 重要对话可以手动设置为不过期                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 过期消息清理策略

```javascript
// MongoDB TTL Index 设置
db.private_messages.createIndex(
    { "server_time": 1 },
    { expireAfterSeconds: 31536000 }  // 365天 = 365 * 24 * 3600
)

db.group_messages.createIndex(
    { "server_time": 1 },
    { expireAfterSeconds: 31536000 }
)

// 定期归档任务 (每日执行)
db.private_messages.aggregate([
    { $match: { server_time: { $lt: ISODate("2025-01-01") } } },
    { $out: "private_messages_archive" }  // 归档到新集合
])
```

### 7.3 会话数据生命周期

```
会话列表数据 (PostgreSQL chat_dialog 表)：
│
├── 对话创建 → 插入 chat_dialog
│
├── 对话更新 → 更新 last_msg_preview, last_msg_ts, unread_count
│
├── 对话归档条件：
│   ├── 双方均未访问超过 6 个月
│   └── 且无未读消息
│
└── 对话删除 → 软删除 (status = 0)
```

## 八、ER 关系图

### 8.1 核心实体关系

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    user    │       │   friend    │       │ friend_apply │
│─────────────│       │─────────────│       │─────────────│
│ uid (PK)   │──┐    │ id (PK)     │    ┌─│ id (PK)     │
│ name        │  │    │ self_id (FK)│────┘ │ from_uid    │
│ email       │  │    │ friend_id   │       │ to_uid      │
│ pwd_hash    │  └──►│ back (备注) │       │ status      │
│ nick        │       └─────────────┘       └─────────────┘
│ icon        │
└─────────────┘       ┌─────────────┐       ┌─────────────┐
       │               │ chat_group  │       │ chat_group │
       │               │─────────────│       │  _member    │
       │               │ group_id(PK)│───────│─────────────│
       │               │ group_code  │       │ id (PK)     │
       │               │ name        │  ┌───│ group_id(FK)│
       │               │ owner_uid   │──┘   │ uid (FK)    │
       │               │ announcement │       │ role        │
       └───────────────└─────────────┘       │ mute_until  │
                           │                 │ join_source │
                           │                 └─────────────┘
                           ▼
              ┌─────────────────────┐
              │   chat_private_msg  │
              │─────────────────────│
              │ msg_id (PK)         │
              │ conv_uid_min        │
              │ conv_uid_max        │
              │ from_uid            │
              │ to_uid              │
              │ created_at          │
              │ group_seq (群内序号) │
              └─────────────────────┘
```

### 8.2 消息存储分离策略

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         消息存储架构                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  PostgreSQL (chat_private_msg / chat_group_msg)                          │
│  ────────────────────────────────────────────────────────────────────────    │
│  │ 存储消息索引：msg_id, sender, receiver, time, group_seq 等              │
│  │ 用于：列表查询、分页、历史记录定位、消息搜索                              │
│  │ 优点：事务保证、复杂查询支持、索引优化                                    │
│                                                                              │
│  MongoDB (private_messages / group_messages)                              │
│  ────────────────────────────────────────────────────────────────────────    │
│  │ 存储消息内容：content, extra, media_url, reactions 等                   │
│  │ 用于：消息详情查询、消息内容搜索、富媒体消息                              │
│  │ 优点：schema 灵活、高写入性能、水平扩展简单                              │
│                                                                              │
│  为什么分开存储？                                                           │
│  ────────────────────────────────────────────────────────────────────────    │
│  │ 1. PostgreSQL 适合结构化的索引数据，支持复杂 JOIN                       │
│  │ 2. MongoDB 适合非结构化的内容数据，灵活 schema                         │
│  │ 3. 避免 PostgreSQL 单表过大导致的性能问题                               │
│  │ 4. 分离后可以独立优化各自的查询模式                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 九、常用查询示例

### 9.1 PostgreSQL 查询示例

```sql
-- 查询用户的所有好友
SELECT u.uid, u.name, u.nick, u.icon, f.back
FROM friend f
JOIN "user" u ON u.uid = f.friend_id
WHERE f.self_id = 12345 AND f.self_id < f.friend_id;

-- 查询用户的群组列表
SELECT cg.group_id, cg.name, cg.icon, cgm.role, cgm.join_source
FROM chat_group_member cgm
JOIN chat_group cg ON cg.group_id = cgm.group_id
WHERE cgm.uid = 12345 AND cgm.status = 1;

-- 查询私聊历史 (分页)
SELECT msg_id, from_uid, to_uid, content, created_at
FROM chat_private_msg
WHERE conv_uid_min = LEAST(12345, 67890)
  AND conv_uid_max = GREATEST(12345, 67890)
ORDER BY created_at DESC
LIMIT 100 OFFSET 0;

-- 查询待处理的好友申请
SELECT fa.*, u.name, u.nick, u.icon
FROM friend_apply fa
JOIN "user" u ON u.uid = fa.from_uid
WHERE fa.to_uid = 12345 AND fa.status = 0
ORDER BY fa.id DESC;

-- 查询群成员
SELECT cgm.uid, u.name, u.nick, u.icon, cgm.role, cgm.join_source
FROM chat_group_member cgm
JOIN "user" u ON u.uid = cgm.uid
WHERE cgm.group_id = 10001 AND cgm.status = 1
ORDER BY cgm.role DESC, cgm.joined_at ASC;

-- 统计用户未读消息数
SELECT 
    (SELECT COUNT(*) FROM chat_private_msg 
     WHERE to_uid = 12345 AND created_at > 
         (SELECT COALESCE(read_ts, 0) FROM chat_private_read_state 
          WHERE uid = 12345))) AS private_unread,
    (SELECT COUNT(*) FROM chat_group_msg cgm
     WHERE cgm.group_id IN (SELECT group_id FROM chat_group_member WHERE uid = 12345)
       AND cgm.created_at >
         (SELECT COALESCE(read_ts, 0) FROM chat_group_read_state 
          WHERE uid = 12345 AND group_id = cgm.group_id))) AS group_unread;
```

### 9.2 MongoDB 查询示例

```javascript
// 查询私聊消息详情
db.private_messages.findOne({ msg_id: "msg-uuid-789" })

// 查询最近 50 条私聊历史
db.private_messages.find({
    $or: [
        { from_uid: 12345, to_uid: 67890 },
        { from_uid: 67890, to_uid: 12345 }
    ]
}).sort({ server_time: -1 }).limit(50)

// 查询群聊消息
db.group_messages.find({
    group_id: 10001,
    server_time: { $gt: 1711180800000 }
}).sort({ server_time: 1 }).limit(100)

// 搜索消息内容
db.private_messages.find({
    $and: [
        { from_uid: 12345, to_uid: 67890 },
        { content: { $regex: "keyword" } }
    ]
}).sort({ server_time: -1 })

// 聚合统计用户发送的消息数量
db.private_messages.aggregate([
    { $match: { from_uid: 12345 } },
    { $group: { 
        _id: null,
        total: { $sum: 1 },
        by_type: { $push: "$msg_type" }
    }}
])

// 批量插入消息
db.private_messages.insertMany([
    { msg_id: "msg-001", from_uid: 12345, to_uid: 67890, content: "Hello", ... },
    { msg_id: "msg-002", from_uid: 12345, to_uid: 67890, content: "How are you?", ... }
])
```

### 9.3 Redis 查询示例

```bash
# 查看用户在线状态
redis-cli GET online:12345

# 查看用户所在的 ChatServer
redis-cli GET chatserver:12345

# 获取用户好友列表
redis-cli SMEMBERS friend:12345

# 获取对话列表 (Sorted Set)
redis-cli ZREVRANGE dialogs:12345 0 19 WITHSCORES

# 获取消息详情缓存
redis-cli GET msg:msg-uuid-789

# 获取热点消息
redis-cli ZREVRANGE hot_messages 0 99 WITHSCORES

# 统计 ChatServer 在线人数
redis-cli SCARD chatservers:chatserver1:online_users

# 获取所有在线用户
redis-cli SMEMBERS chatservers:chatserver1:online_users

# 发布好友上线通知
redis-cli PUBLISH presence:67890 '{"event":"online","server":"chatserver1"}'

# 订阅好友上线通知
redis-cli SUBSCRIBE presence:67890

# 使用 Lua 脚本原子性操作
redis-cli EVAL "
    local count = redis.call('INCR', KEYS[1])
    if count == 1 then
        redis.call('EXPIRE', KEYS[1], ARGV[1])
    end
    return count
" 1 user:msg:count 300
```

## 十、性能优化指南

### 10.1 PostgreSQL 性能优化

```sql
-- 1. 分析慢查询
EXPLAIN ANALYZE 
SELECT * FROM chat_private_msg 
WHERE conv_uid_min = 12345 AND conv_uid_max = 67890 
ORDER BY created_at DESC LIMIT 100;

-- 2. 查看表统计信息
SELECT schemaname, tablename, n_live_tup, n_dead_tup, last_vacuum, last_autovacuum
FROM pg_stat_user_tables
WHERE tablename IN ('chat_private_msg', 'chat_group_msg', 'friend', 'chat_group_member');

-- 3. 查看索引使用情况
SELECT indexrelname, idx_scan, idx_tup_read, idx_tup_fetch
FROM pg_stat_user_indexes
WHERE schemaname = 'public'
ORDER BY idx_scan DESC;

-- 4. VACUUM 清理 dead tuples
VACUUM ANALYZE chat_private_msg;

-- 5. 重构大表分区 (按月分区)
CREATE TABLE chat_private_msg_partitioned (
    LIKE chat_private_msg INCLUDING ALL
) PARTITION BY RANGE (created_at);

CREATE TABLE chat_private_msg_2026_01 
    PARTITION OF chat_private_msg_partitioned
    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');
```

### 10.2 MongoDB 性能优化

```javascript
// 1. 查看慢查询日志
db.getProfilingLevel()
db.setProfilingLevel(1, { slowms: 100 })

// 2. 查看查询计划
db.private_messages.find({
    from_uid: 12345, 
    to_uid: 67890,
    server_time: { $gt: 1711180800000 }
}).sort({ server_time: -1 }).limit(100).explain("executionStats")

// 3. 强制使用索引
db.private_messages.find({
    from_uid: 12345, 
    to_uid: 67890
}).hint({ from_uid: 1, to_uid: 1, server_time: -1 })

// 4. 分析数据分布
db.private_messages.aggregate([
    { $group: { _id: "$from_uid", count: { $sum: 1 } } },
    { $sort: { count: -1 } },
    { $limit: 10 }
])

// 5. 内存限制和淘汰策略
db.adminCommand({ setParameter: 1, internalQueryExecMaxBlockingSortBytes: 33554432 })

// 6. 连接池配置
mongocli --host 127.0.0.1:27017 --eval '
    db.adminCommand({ getParameter: 1, "net.maxIncomingConnections" })
'
```

### 10.3 Redis 性能优化

```bash
# 1. 查看慢查询
redis-cli SLOWLOG GET 10

# 2. 查看内存使用
redis-cli INFO memory

# 3. 查看 Key 数量
redis-cli DBSIZE

# 4. 查看大 Key
redis-cli --bigkeys

# 5. 内存优化 - 使用ziplist
redis-cli CONFIG SET hash-max-ziplist-entries 512
redis-cli CONFIG SET hash-max-ziplist-value 64

# 6. 查看客户端连接
redis-cli CLIENT LIST | awk -F'|' '{print $1}' | cut -d' ' -f2-

# 7. 内存碎片整理
redis-cli MEMORY PURGE

# 8. Key 过期策略检查
redis-cli --scan --pattern 'online:*' | xargs -I{} redis-cli TTL {}
```

## 十一、多数据库一致性

### 11.1 Cache-Aside 模式

```
读取流程：
┌─────────────────────────────────────────────────────────────┐
│  1. 应用先查 Redis 缓存                                    │
│  2. Cache Miss → 查 PostgreSQL                            │
│  3. 写入 Redis，设置 TTL                                   │
│  4. 返回数据                                               │
└─────────────────────────────────────────────────────────────┘

写入流程：
┌─────────────────────────────────────────────────────────────┐
│  1. 先更新 PostgreSQL (主数据库)                           │
│  2. 删除 Redis Key (而非更新)                              │
│  3. 下次读请求会重新加载最新数据                            │
└─────────────────────────────────────────────────────────────┘

为什么删除而非更新？
┌─────────────────────────────────────────────────────────────┐
│  原因 1: 避免并发覆盖                                      │
│         线程A: READ → modify → WRITE                      │
│         线程B: READ → modify → WRITE                      │
│         如果用 UPDATE，线程B的数据可能覆盖线程A的            │
│         用 DELETE，下次重新 LOAD，确保一致性                 │
│                                                              │
│  原因 2: 减少 ABA 问题                                     │
│         删除后又被设置，很难发生问题                         │
│                                                              │
│  原因 3: 简单有效                                          │
│         删除操作总是幂等的                                  │
└─────────────────────────────────────────────────────────────┘
```

### 11.2 消息双写一致性

```
消息发送流程（保证 PostgreSQL 和 MongoDB 一致）：
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  BEGIN TRANSACTION                                          │
│  1. INSERT INTO chat_private_msg (PostgreSQL)              │
│  2. INSERT INTO private_messages (MongoDB)                 │
│  COMMIT                                                     │
│                                                              │
│  如果任一步失败，回滚事务                                   │
│  如果两步都成功，发送 ACK 给客户端                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘

失败重试策略：
┌─────────────────────────────────────────────────────────────┐
│  第一次失败: 重试 3 次，间隔 1s                             │
│  第二次失败: 重试 3 次，间隔 5s                             │
│  第三次失败: 写入 outbox 表，等待异步补偿                    │
└─────────────────────────────────────────────────────────────┘
```

### 11.3 Outbox 模式实现

```sql
-- chat_event_outbox 表用于可靠事件发布
CREATE TABLE chat_event_outbox (
    event_id varchar(64) PRIMARY KEY,
    event_type varchar(32) NOT NULL,
    topic varchar(64) NOT NULL,
    payload jsonb NOT NULL,
    status smallint NOT NULL DEFAULT 0,  -- 0=待发送 1=已发送 2=失败
    retry_count integer NOT NULL DEFAULT 0,
    created_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sent_at timestamptz
);

-- 应用写入消息时，同时写入 outbox
BEGIN;
INSERT INTO chat_private_msg (...) VALUES (...);
INSERT INTO chat_private_messages (...) VALUES (...);
INSERT INTO chat_event_outbox (event_id, event_type, topic, payload)
VALUES ('evt-001', 'private_message', 'chat.private.v1', '{"msg_id":"..."}');
COMMIT;

-- 异步任务扫描 outbox，发布到 Kafka
SELECT * FROM chat_event_outbox 
WHERE status = 0 AND retry_count < 3
ORDER BY created_at ASC
LIMIT 100;

-- 发布成功后更新状态
UPDATE chat_event_outbox 
SET status = 1, sent_at = CURRENT_TIMESTAMP
WHERE event_id = 'evt-001';
```

## 十二、监控与告警

### 12.1 数据库关键监控指标

#### PostgreSQL 监控

| 指标 | 正常范围 | 告警阈值 | 说明 |
|------|----------|----------|------|
| `pg_database_size` | - | > 100GB | 数据库大小 |
| `pg_stat_user_tables.n_live_tup` | - | 异常增长/下降 | 表行数 |
| `pg_stat_user_tables.n_dead_tup` | < 1000 | > 10000 | 死亡元组 |
| `pg_stat_activity.count` | < 100 | > 200 | 活跃连接数 |
| `pg_replication.lag` | < 1MB | > 10MB | 复制延迟 |
| `pg_wal_lsn.diff` | < 16MB | > 64MB | WAL 堆积 |

#### MongoDB 监控

| 指标 | 正常范围 | 告警阈值 | 说明 |
|------|----------|----------|------|
| `connections.current` | < 500 | > 1000 | 当前连接数 |
| `opcounters.insert` | - | 突降/突升 | 写入速率 |
| `opcounters.query` | - | 突降/突升 | 查询速率 |
| `metrics.cursor.openTotal` | - | 持续增长 | 游标数量 |
| `extra_info.page_faults` | < 100/s | > 1000/s | 页面错误 |

#### Redis 监控

| 指标 | 正常范围 | 告警阈值 | 说明 |
|------|----------|----------|------|
| `used_memory_human` | < 80% maxmemory | > 90% maxmemory | 内存使用 |
| `connected_clients` | < 1000 | > 5000 | 客户端连接 |
| `instantaneous_ops_per_sec` | 10000-100000 | < 1000 | 操作速率 |
| `keyspace_hits_per_sec` | > 95% hit rate | < 90% hit rate | 命中率 |

### 12.2 Prometheus 告警规则

```yaml
# PostgreSQL 告警
groups:
  - name: postgres_alerts
    rules:
      - alert: PostgresConnectionHigh
        expr: sum(pg_stat_activity_count) > 150
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "PostgreSQL connections high"
          
      - alert: PostgresReplicationLag
        expr: pg_replication_lag_bytes > 10485760
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "PostgreSQL replication lag > 10MB"

# MongoDB 告警
  - name: mongodb_alerts
    rules:
      - alert: MongoConnectionExhausted
        expr: mongodb_connections_current > 800
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "MongoDB connections exhausted"

# Redis 告警
  - name: redis_alerts
    rules:
      - alert: RedisMemoryHigh
        expr: redis_memory_used_bytes / redis_memory_max_bytes > 0.9
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Redis memory usage > 90%"
```

## 十三、备份与恢复

### 13.1 PostgreSQL 备份恢复

```bash
# 全量备份
pg_dump -h 127.0.0.1 -U memochat -d memo_pg -Fc -f backup_$(date +%Y%m%d).dump

# 增量备份 (基于 WAL)
mkdir -p /backup/wal
pg_basebackup -h 127.0.0.1 -U repl -D /backup/wal -Ft -z -P -X stream

# 恢复全量备份
pg_restore -h 127.0.0.1 -U memochat -d memo_pg -c backup_20260410.dump

# 恢复指定表
pg_restore -h 127.0.0.1 -U memochat -d memo_pg -t chat_private_msg backup.dump

# 点时间恢复 (PITR)
pg_restore -h 127.0.0.1 -U memochat -d memo_pg --wal-target-time="2026-04-10 10:00:00" backup.dump
```

### 13.2 MongoDB 备份恢复

```bash
# 全量备份
mongodump --host 127.0.0.1 --port 27017 --db memo --out /backup/mongo_$(date +%Y%m%d)

# 备份指定集合
mongodump --host 127.0.0.1 --port 27017 --db memo --collection private_messages --out /backup

# 增量备份 (oplog)
mongodump --host 127.0.0.1 --port 27017 --oplog --out /backup/oplog_$(date +%Y%m%d)

# 恢复
mongorestore --host 127.0.0.1 --port 27017 --db memo /backup/memo

# 恢复前删除目标集合
mongorestore --host 127.0.0.1 --port 27017 --db memo --drop /backup/memo

# 部分恢复
mongorestore --host 127.0.0.1 --port 27017 --db memo --collection private_messages --query '{"server_time": {"$gt": 1710000000000}}' /backup
```

### 13.3 Redis 备份恢复

```bash
# RDB 快照备份
redis-cli SAVE
# 或后台保存
redis-cli BGSAVE

# 查看保存状态
redis-cli LASTSAVE

# AOF 备份
redis-cli BGREWRITEAOF

# 恢复 RDB 备份
cp /var/lib/redis/dump.rdb /path/to/backup/
redis-cli SHUTDOWN NOSAVE
cp /path/to/backup/dump.rdb /var/lib/redis/
redis-server

# 从 AOF 恢复
redis-cli SHUTDOWN NOSAVE
redis-check-aof --fix /var/lib/redis/appendonly.aof
redis-server
```

### 13.4 定时备份策略

```bash
#!/bin/bash
# scripts/backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR=/backup/$DATE

mkdir -p $BACKUP_DIR

# PostgreSQL 备份
pg_dump -h 127.0.0.1 -U memochat -d memo_pg -Fc -f $BACKUP_DIR/postgres.dump

# MongoDB 备份
mongodump --host 127.0.0.1 --port 27017 --db memo --out $BACKUP_DIR/mongo

# Redis RDB 备份
cp /var/lib/redis/dump.rdb $BACKUP_DIR/redis.rdb

# 压缩
tar -czf $BACKUP_DIR.tar.gz $BACKUP_DIR

# 上传到对象存储
aws s3 cp $BACKUP_DIR.tar.gz s3://memochat-backup/

# 清理 7 天前的备份
find /backup -type f -mtime +7 -delete
```

### 13.5 备份恢复演练

```powershell
# scripts/backup_drill.ps1

# 1. 创建测试环境
docker run -d --name memochat-restore-test -e POSTGRES_PASSWORD=test123 postgres:16

# 2. 恢复 PostgreSQL 备份
docker exec -i memochat-restore-test psql -U postgres < backup_postgres.dump

# 3. 验证数据完整性
docker exec memochat-restore-test psql -U postgres -d memo_pg -c "SELECT COUNT(*) FROM \"user\";"
docker exec memochat-restore-test psql -U postgres -d memo_pg -c "SELECT COUNT(*) FROM friend;"

# 4. 清理测试环境
docker stop memochat-restore-test
docker rm memochat-restore-test
```
