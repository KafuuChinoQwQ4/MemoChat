receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
  zipkin:
    endpoint: 0.0.0.0:9411
  filelog:
    include:
      - /workspace/Memo_ops/runtime/artifacts/logs/**/*.json
      - /workspace/Memo_ops/runtime/services/*/logs/*.json
      - /workspace/build/bin/**/logs/*.json
    exclude:
      - /workspace/**/data/**
      - /workspace/**/credentials/**
      - /workspace/**/cookies/**
      - /workspace/**/sessions/**
    start_at: end
    operators:
      - type: json_parser
        parse_from: body

  filelog/envoy:
    include:
      - /workspace/envoy/logs/access.json
    start_at: end
    operators:
      - type: json_parser
        parse_from: body
        parse_to: attributes

extensions:
  health_check:
    endpoint: 0.0.0.0:13133
    path: /healthz
    # The distroless image has no HTTP client. This safe static config lets the
    # collector's own HTTP config provider turn the live endpoint into a probe.
    response_body:
      healthy: |
        receivers:
          otlp:
            protocols:
              grpc: {}
        exporters:
          debug: {}
        service:
          pipelines:
            traces:
              receivers: [otlp]
              exporters: [debug]

processors:
  attributes/envoy_loki:
    actions:
      - key: service
        value: memochat-envoy-gateway
        action: upsert
      - key: component
        value: envoy_gateway
        action: upsert
      - key: loki.attribute.labels
        value: service,component
        action: insert
  batch:

exporters:
  debug:
    verbosity: normal
  otlp/tempo:
    endpoint: memochat-tempo:4317
    tls:
      insecure: true
  loki:
    endpoint: http://memochat-loki:3100/loki/api/v1/push

service:
  extensions: [health_check]
  telemetry:
    metrics:
      address: 0.0.0.0:9464
  pipelines:
    traces:
      receivers: [otlp, zipkin]
      processors: [batch]
      exporters: [otlp/tempo, debug]
    logs:
      receivers: [filelog]
      processors: [batch]
      exporters: [loki, debug]
    logs/envoy:
      receivers: [filelog/envoy]
      processors: [attributes/envoy_loki, batch]
      exporters: [loki]
