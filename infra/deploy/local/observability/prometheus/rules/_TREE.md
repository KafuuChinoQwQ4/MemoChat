# rules/ 目录树

> Prometheus 告警/记录规则定义（按服务分组）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `ai_orchestrator.yml` | AI 编排服务告警规则组。 |
| `envoy_gateway.yml` | Envoy 网关告警规则组。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
