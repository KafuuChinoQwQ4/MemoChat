{{- if .Values.jobs.postgresMigrate.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: memochat-postgres-migrate
  namespace: {{ .Values.namespaces.prod }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    spec:
      restartPolicy: OnFailure
      automountServiceAccountToken: false
      securityContext:
        {{- include "memochat.podSecurityContext" . | nindent 8 }}
      containers:
        - name: postgres-migrate
          image: {{ .Values.jobs.postgresMigrate.image | quote }}
          securityContext:
            {{- include "memochat.containerSecurityContext" $ | nindent 12 }}
          env:
            - name: PGPASSWORD
              valueFrom: { secretKeyRef: { name: memochat-secrets, key: postgres-password } }
          command:
            - sh
            - -c
            - >
              psql -v ON_ERROR_STOP=1 -h {{ .Values.externalServices.postgres.host }} -p {{ .Values.externalServices.postgres.port }}
              -U {{ .Values.externalServices.postgres.user }} -d memo_account
              -f /migrations/009_memo_account_schema.sql &&
              psql -v ON_ERROR_STOP=1 -h {{ .Values.externalServices.postgres.host }} -p {{ .Values.externalServices.postgres.port }}
              -U {{ .Values.externalServices.postgres.user }} -d memo_account
              -f /migrations/013_r18_access_policy.sql &&
              psql -v ON_ERROR_STOP=1 -h {{ .Values.externalServices.postgres.host }} -p {{ .Values.externalServices.postgres.port }}
              -U {{ .Values.externalServices.postgres.user }} -d {{ .Values.externalServices.postgres.database }}
              -f /migrations/001_business_baseline.sql &&
              psql -v ON_ERROR_STOP=1 -h {{ .Values.externalServices.postgres.host }} -p {{ .Values.externalServices.postgres.port }}
              -U {{ .Values.externalServices.postgres.user }} -d {{ .Values.externalServices.postgres.database }}
              -f /migrations/002_chat_event_outbox.sql &&
              psql -v ON_ERROR_STOP=1 -h {{ .Values.externalServices.postgres.host }} -p {{ .Values.externalServices.postgres.port }}
              -U {{ .Values.externalServices.postgres.user }} -d {{ .Values.externalServices.postgres.database }}
              -f /migrations/001_memo_ops_baseline.sql
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: init
              mountPath: /migrations
      volumes:
        - name: tmp
          emptyDir: {}
        - name: init
          configMap:
            name: memochat-platform-init
{{- end }}
---
{{- if .Values.jobs.kafkaInit.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: memochat-kafka-init
  namespace: {{ .Values.namespaces.prod }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    spec:
      restartPolicy: OnFailure
      automountServiceAccountToken: false
      securityContext:
        {{- include "memochat.podSecurityContext" . | nindent 8 }}
      containers:
        - name: kafka-init
          image: {{ .Values.jobs.kafkaInit.image | quote }}
          securityContext:
            {{- include "memochat.containerSecurityContext" $ | nindent 12 }}
          env:
            - name: PYTHONUSERBASE
              value: /tmp/python-user
            - name: KAFKA_BROKERS
              value: {{ .Values.externalServices.kafka.brokers | quote }}
          command: ["python", "/init/init-kafka.py"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: init
              mountPath: /init
      volumes:
        - name: tmp
          emptyDir: {}
        - name: init
          configMap:
            name: memochat-platform-init
{{- end }}
---
{{- if .Values.jobs.rabbitmqInit.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: memochat-rabbitmq-init
  namespace: {{ .Values.namespaces.prod }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    spec:
      restartPolicy: OnFailure
      automountServiceAccountToken: false
      securityContext:
        {{- include "memochat.podSecurityContext" . | nindent 8 }}
      containers:
        - name: rabbitmq-init
          image: {{ .Values.jobs.rabbitmqInit.image | quote }}
          securityContext:
            {{- include "memochat.containerSecurityContext" $ | nindent 12 }}
          env:
            - name: PYTHONUSERBASE
              value: /tmp/python-user
            - name: RABBITMQ_HOST
              value: {{ .Values.externalServices.rabbitmq.host | quote }}
            - name: RABBITMQ_PORT
              value: {{ .Values.externalServices.rabbitmq.port | quote }}
            - name: RABBITMQ_USERNAME
              valueFrom: { secretKeyRef: { name: memochat-secrets, key: rabbitmq-username } }
            - name: RABBITMQ_PASSWORD
              valueFrom: { secretKeyRef: { name: memochat-secrets, key: rabbitmq-password } }
            - name: RABBITMQ_VHOST
              value: {{ .Values.externalServices.rabbitmq.vhost | quote }}
          command: ["python", "/init/init-rabbitmq.py"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: init
              mountPath: /init
      volumes:
        - name: tmp
          emptyDir: {}
        - name: init
          configMap:
            name: memochat-platform-init
{{- end }}
