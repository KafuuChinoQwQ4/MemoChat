# shared/ 目录树

> Helm chart 共享模板：命名/标签、生产镜像门禁与工作负载安全上下文 helper。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `_helpers.tpl` | Helm 命名/标签、生产 GHCR commit-SHA 校验、固定 digest 镜像与非 root/只读容器安全 helper。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
