# patches/ 目录树

> 存放 MemoChat libwebsockets overlay 端口需要应用的上游源码补丁。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `enable-h3-webtransport-settings.patch` | 为 HTTP/3 WebTransport provider 打开必要 SETTINGS、CONNECT 200 响应、WT session 标记、peer WT stream 分发和 stream writable 调度。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
