# nghttp2/ 目录树

> nghttp2 1.68.0 的 vcpkg overlay 端口，按固定 SHA-512 获取上游源码并只构建静态库。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `COPYING` | 端口/库的许可证文件。 |
| `portfile.cmake` | 按固定版本和 SHA-512 下载、构建并安装 nghttp2 静态库。 |
| `vcpkg.json` | nghttp2 端口清单（版本/依赖）。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
