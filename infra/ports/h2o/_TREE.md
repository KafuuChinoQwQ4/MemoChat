# h2o/ 目录树

> h2o HTTP 库的 vcpkg 自定义 overlay 端口定义。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `portfile.cmake` | h2o 端口构建脚本。 |
| `vcpkg.json` | h2o 端口清单（版本/依赖）。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
