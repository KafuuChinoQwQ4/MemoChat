# config/ 目录树

> MemoOps 平台配置：采集器、API 服务端与 QML 客户端的配置文件。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `memoops-qml.ini` | QML 客户端配置（连接地址等）。 |
| `opscollector.yaml` | 采集器服务配置。 |
| `opsserver.yaml` | API 服务端配置。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
