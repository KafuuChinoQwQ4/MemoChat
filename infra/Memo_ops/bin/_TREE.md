# bin/ 目录树

> MemoOps 依赖服务的本地启停脚本（MinIO 对象存储）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `start_minio.bat` | 启动 MinIO 的 Windows 批处理脚本。 |
| `stop_minio.bat` | 停止 MinIO 的 Windows 批处理脚本。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
