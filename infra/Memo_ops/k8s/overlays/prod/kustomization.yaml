apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This legacy production overlay is deliberately non-buildable. Production
# deployment is owned by infra/deploy/kubernetes/charts/memochat.
resources:
  - RETIRED_PRODUCTION_OVERLAY_USE_HELM.yaml
