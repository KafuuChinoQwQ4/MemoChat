# base/ 目录树

> MemoOps k8s 基础资源：命名空间、配置、密钥与网络策略。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `configmap.yaml` | 平台通用 ConfigMap。 |
| `networkpolicy.yaml` | 命名空间默认拒绝与服务间最小网络访问 allowlist。 |
| `namespace.yaml` | 命名空间定义。 |
| `secrets.yaml` | 平台 Secret 定义。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
