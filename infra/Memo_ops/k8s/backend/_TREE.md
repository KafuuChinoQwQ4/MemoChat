# backend/ 目录树

> MemoOps k8s 中后端业务服务的部署清单（聊天、网关、验证服务）。

## 文件

| 文件 | 作用概括 |
| --- | --- |
| `chatserver.yaml` | ChatServer 的 k8s 部署清单。 |
| `gateserver.yaml` | GateServer 的 k8s 部署清单。 |
| `varifyserver.yaml` | VarifyServer（验证服务）的 k8s 部署清单。 |

<!-- TREE-DOC: 自动维护。文件夹内容变更时同步更新本表与上面的一句话概括。 -->
